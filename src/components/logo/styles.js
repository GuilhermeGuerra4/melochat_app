import React from "react";
import {StyleSheet} from "react-native";

export const stylesheet = StyleSheet.create({
    image: {
        width: 100,
        height: 200,
        marginBottom: 130,
    }
});